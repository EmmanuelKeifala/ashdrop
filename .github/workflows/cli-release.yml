name: Release CLI

on:
  push:
    tags:
      - "cli-v*"

permissions:
  contents: read

concurrency:
  group: cli-release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
      attestations: write
    defaults:
      run:
        shell: bash
    steps:
      - name: Check out tagged source
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          fetch-tags: true
          persist-credentials: false

      - name: Validate release version
        id: version
        run: |
          set -euo pipefail
          version=$(ashdrop/cli/scripts/release-version.sh "$GITHUB_REF_NAME")
          printf 'version=%s\n' "$version" >>"$GITHUB_OUTPUT"
          printf 'VERSION=%s\n' "$version" >>"$GITHUB_ENV"

      - name: Refuse an existing release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          response=$(mktemp)
          status=$(curl --silent --show-error --location \
            --output "$response" \
            --write-out '%{http_code}' \
            --header 'Accept: application/vnd.github+json' \
            --header "Authorization: Bearer $GH_TOKEN" \
            --header 'X-GitHub-Api-Version: 2022-11-28' \
            "$GITHUB_API_URL/repos/$GITHUB_REPOSITORY/releases/tags/$GITHUB_REF_NAME")
          case "$status" in
            404) ;;
            200)
              printf 'release %s already exists; refusing to overwrite it\n' "$GITHUB_REF_NAME" >&2
              exit 1
              ;;
            *)
              printf 'could not establish release absence (GitHub API HTTP %s)\n' "$status" >&2
              cat "$response" >&2
              exit 1
              ;;
          esac
          rm -f "$response"

      - name: Set up Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        with:
          version: 0.16.0

      - name: Install QEMU user emulation
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install --yes --no-install-recommends qemu-user
          command -v qemu-aarch64 >/dev/null

      - name: Test and build release binaries
        working-directory: ashdrop/cli
        run: |
          set -euo pipefail
          zig version | grep -Fx '0.16.0'
          zig build test -Dversion="$VERSION"
          zig build -Dtarget=x86_64-linux-musl -Dcpu=baseline -Doptimize=ReleaseSafe \
            -Dversion="$VERSION" --prefix zig-out/release/linux-x86_64
          zig build -Dtarget=aarch64-linux-musl -Dcpu=baseline -Doptimize=ReleaseSafe \
            -Dversion="$VERSION" --prefix zig-out/release/linux-aarch64

      - name: Smoke-test release binaries
        working-directory: ashdrop/cli
        run: |
          set -euo pipefail
          test "$(env -u HOME zig-out/release/linux-x86_64/bin/ashdrop --version)" = "ashdrop $VERSION"
          test "$(env -u HOME qemu-aarch64 zig-out/release/linux-aarch64/bin/ashdrop --version)" = "ashdrop $VERSION"

      - name: Package and verify release artifacts
        run: |
          set -euo pipefail
          x86_archive="ashdrop-v$VERSION-linux-x86_64.tar.gz"
          arm_archive="ashdrop-v$VERSION-linux-aarch64.tar.gz"
          mkdir -p dist/stage/linux-x86_64 dist/stage/linux-aarch64
          install -m 0755 ashdrop/cli/zig-out/release/linux-x86_64/bin/ashdrop dist/stage/linux-x86_64/ashdrop
          install -m 0755 ashdrop/cli/zig-out/release/linux-aarch64/bin/ashdrop dist/stage/linux-aarch64/ashdrop
          tar --sort=name --owner=0 --group=0 --numeric-owner --mtime='UTC 1970-01-01' \
            --mode='u=rwx,go=rx' -C dist/stage/linux-x86_64 -czf "dist/$x86_archive" ashdrop
          tar --sort=name --owner=0 --group=0 --numeric-owner --mtime='UTC 1970-01-01' \
            --mode='u=rwx,go=rx' -C dist/stage/linux-aarch64 -czf "dist/$arm_archive" ashdrop
          test "$(tar -tzf "dist/$x86_archive")" = 'ashdrop'
          test "$(tar -tzf "dist/$arm_archive")" = 'ashdrop'
          cd dist
          sha256sum "$arm_archive" "$x86_archive" >SHA256SUMS
          sha256sum --check SHA256SUMS

      - name: Attest release archives
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
        with:
          subject-path: dist/*.tar.gz

      - name: Final release tag guard
        run: |
          set -euo pipefail
          tag_ref="refs/tags/$GITHUB_REF_NAME"
          check_ref='refs/ashdrop-release-check/current'
          git update-ref -d "$check_ref"
          git fetch --no-tags --force origin "$tag_ref:$check_ref"
          current_commit=$(git rev-parse --verify "$check_ref^{commit}")
          if [[ "$current_commit" != "$GITHUB_SHA" ]]; then
            printf 'release tag %s moved: expected %s, found %s\n' \
              "$GITHUB_REF_NAME" "$GITHUB_SHA" "$current_commit" >&2
            exit 1
          fi

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          x86_archive="ashdrop-v$VERSION-linux-x86_64.tar.gz"
          arm_archive="ashdrop-v$VERSION-linux-aarch64.tar.gz"
          gh release create "$GITHUB_REF_NAME" \
            "dist/$x86_archive" \
            "dist/$arm_archive" \
            dist/SHA256SUMS \
            --repo "$GITHUB_REPOSITORY" \
            --verify-tag \
            --title "Ashdrop CLI v$VERSION" \
            --notes "Linux x86_64 and ARM64 binaries for Ashdrop CLI v$VERSION. Verify downloads with SHA256SUMS and GitHub artifact attestations."

      - name: Promote CLI channel
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          channel_ref='refs/heads/cli-channel'
          channel_tracking_ref='refs/remotes/origin/cli-channel'

          set +e
          git ls-remote --exit-code --heads origin "$channel_ref" >/dev/null
          channel_status=$?
          set -e
          case "$channel_status" in
            0)
              git fetch --no-tags origin "$channel_ref:$channel_tracking_ref"
              channel_base=$channel_tracking_ref
              ;;
            2) channel_base=$GITHUB_SHA ;;
            *)
              printf 'could not inspect CLI channel (git status %s)\n' "$channel_status" >&2
              exit 1
              ;;
          esac

          channel_parent=$(mktemp -d)
          channel_dir=$channel_parent/worktree
          cleanup_channel() {
            git worktree remove --force "$channel_dir" 2>/dev/null || true
            rm -rf "$channel_parent"
          }
          trap cleanup_channel EXIT

          git worktree add --detach "$channel_dir" "$channel_base"
          rm -f "$channel_dir/cli-version"
          printf '%s\n' "$VERSION" >"$channel_dir/cli-version"
          git -C "$channel_dir" add cli-version
          git -C "$channel_dir" \
            -c user.name='github-actions[bot]' \
            -c user.email='41898282+github-actions[bot]@users.noreply.github.com' \
            commit -m "chore: promote CLI channel to v$VERSION"
          channel_commit=$(git -C "$channel_dir" rev-parse HEAD)

          askpass=$channel_parent/askpass.sh
          cat >"$askpass" <<'EOF'
          #!/bin/sh
          case $1 in
            *Username*) printf '%s\n' x-access-token ;;
            *Password*) printf '%s\n' "$GH_TOKEN" ;;
            *) exit 1 ;;
          esac
          EOF
          chmod 0700 "$askpass"
          GIT_ASKPASS=$askpass GIT_TERMINAL_PROMPT=0 \
            git push origin "$channel_commit:refs/heads/cli-channel"
